# Third-Party Notices

This repository is Rust-only. Third-party dependencies are locked in
`Cargo.toml` / `Cargo.lock` (workspace) and `desktop/src-tauri/Cargo.toml` /
`desktop/src-tauri/Cargo.lock` (Tauri shell); licenses follow crates.io
metadata (predominantly MIT / Apache-2.0).

The former `wreq-js` native-transport notices belonged to the frozen JS tree
and stay with the reference: archived OmniRoute-Rust repository and
`~/ai-workstation/Archives/OmniRoute/`.
